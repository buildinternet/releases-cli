/**
 * API response types for the Releases registry.
 *
 * Re-exported from `@buildinternet/releases-api-types`, the single source of
 * truth for the wire protocol. Published from the monorepo
 * (`buildinternet/releases`, `packages/api-types/`); bump the pin in
 * `package.json` when adopting new response shapes.
 */
export * from "@buildinternet/releases-api-types";
