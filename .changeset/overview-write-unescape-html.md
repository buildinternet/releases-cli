---
"@buildinternet/releases": patch
---

`releases admin overview-write` now accepts `--unescape-html`, which runs a small pre-upload pass to decode `&amp;`, `&lt;`, `&gt;`, `&quot;`, and `&#39;`. Useful when the markdown was generated by a sub-agent that escaped entities reflexively.
