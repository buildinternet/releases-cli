---
"@buildinternet/releases": patch
---

Bump `@buildinternet/releases-api-types` from `^0.24.0` to `^0.27.0` and `commander` from `^14.0.3` to `^15.0.0` (ESM-only; no API changes required for this CLI).
