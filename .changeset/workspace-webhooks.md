---
"@buildinternet/releases": minor
---

Add `--workspace <id-or-slug>` to `releases webhook list/add/show/edit/remove/test/rotate-secret` for managing a shared workspace-owned webhook instead of your own, and a new `releases workspace list` command to find a workspace's id, slug, and your role in it (#406).
